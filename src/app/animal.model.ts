export class Animal {
  constructor (public name: string, public owner: string, public breed: string, public description: string, public image: string, public category: string, public location: string, public goal: number, public id: number) {
      
  }
}
